# Airbnb_Superhost_Classifier_ML
A machine learning project from Break Through Tech that uses Logistic Regression and model selection techniques to build and evaluate a binary classification model. The project includes data preprocessing, hyperparameter tuning, performance evaluation, and model serialization using Pickle.
